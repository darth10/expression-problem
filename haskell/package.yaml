name:                ExpressionProblem
version:             0.1.0.0
github:              "darth10/expression-problem"
license:             GPL-2
author:              "Akhil Wali"
maintainer:          "akhil.wali.10@gmail.com"
copyright:           "2020 Akhil Wali"

# Metadata used when publishing your package
# synopsis:            Short description of your package
# category:            Web

# To avoid duplicated efforts in documentation and dealing with the
# complications of embedding Haddock markup inside cabal files, it is
# common to point users to the README.md file.
description:         Please see the README on GitHub at <https://github.com/githubuser/ExpressionProblem#readme>

dependencies:
- base >= 4.7 && < 5
- HUnit >= 1.6.0.0

library:
  source-dirs: src

tests:
  ExpressionProblem-test:
    main:                Spec.hs
    source-dirs:         test
    ghc-options:
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N
    dependencies:
    - ExpressionProblem
